---
/** A gold hairline with a centred ornament, used as a section divider. */
interface Props {
  label?: string;
}
const { label } = Astro.props;
---

<hr class="rule" aria-hidden="true" />
{label && <span class="visually-hidden">{label}</span>}
